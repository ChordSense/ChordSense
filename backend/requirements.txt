Flask==3.0.3
Werkzeug==3.0.3
numpy
librosa==0.11.0
scipy

# Record mode uses the system-provided HailoRT Python bindings and its compiled
# HEF. PyTorch remains in models/chordsense_cnn/requirements.txt for training,
# checkpoint evaluation, and the optional Torch inference backend.
